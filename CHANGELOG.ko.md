# 변경 이력

## 0.2.0

- `programmersHelper.language` 설정을 추가하고 C++/Java 풀이 파일, 초기 템플릿, 풀이 기록을 언어별로 관리하도록 확장했습니다.
- Java 템플릿 fetch, `Solution.java`/`TestRunner.java` 생성, `javac`/`java` 기반 샘플 및 커스텀 테스트 실행을 추가했습니다.
- 언어별 runner builder와 에러 진단 파싱 구조를 도입해 이후 지원 언어를 추가하기 쉽게 정리했습니다.
- 풀이기록에서 현재 언어 기록을 먼저 보여주고 다른 언어 기록은 접힌 섹션으로 분리해 볼 수 있게 했습니다.
- helper Docker 런타임을 C++/Java 도구와 UTF-8 locale을 포함한 `programmers-helper-runtime:2`로 갱신해 한글 문제 경로의 Java 컴파일을 지원합니다.
- `package.json`과 `package-lock.json` 버전을 `0.2.0`으로 올렸습니다.

## 0.1.1

- Windows 로컬 실행에서 생성된 C++ 테스트 러너가 `.exe` 바이너리로 빌드되도록 수정했습니다.
- 로컬 테스트 러너에서는 POSIX 전용 메모리 측정 코드를 생성하지 않아 Windows 빌드가 `sys/resource.h`를 포함하지 않도록 했습니다.
- Windows에서는 Docker 실행 모드를 권장하고, 로컬 실행에는 MinGW `g++` 또는 LLVM `clang++`가 필요하다는 안내를 추가했습니다.
- `package.json`과 `package-lock.json` 버전을 `0.1.1`로 올렸습니다.

## 0.1.0

- 기본 키보드 단축키를 추가했습니다: 샘플 테스트 실행은 `Ctrl+Alt+T`, 실행 중지는 `Ctrl+Alt+S`입니다.
- 샘플 테스트 실행, 실행 중지, 커스텀 테스트 실행, 메모 열기를 VS Code 명령으로 노출해 사용자가 원하는 키를 지정할 수 있게 했습니다.
- 문제 번호 입력창에서 `Enter`를 누르면 `문제 생성 및 열기` 버튼과 같은 동작을 하도록 했습니다.
- 문제 열기, 리뷰 변경, 삭제, 풀이기록 변경 후 사이드바 문제 목록 캐시를 무효화하지 않고 갱신된 인덱스 목록으로 바로 교체하도록 개선했습니다.
- Webview 렌더링에서 문제 목록과 풀이기록 row를 key 기반으로 재사용해 HTML 재파싱과 DOM 재생성을 줄였습니다.
- 주요 JavaScript 모듈에 책임을 드러내는 주석을 보강하고, store, runner, Docker, parsing, rendering 흐름을 더 작은 책임 단위로 정리했습니다.
- `package.json`과 `package-lock.json` 버전을 `0.1.0`으로 올렸습니다.

## 0.0.18

- 이전 `0.0.17` 배포 때 누락된 manifest 버전 반영을 바로잡아 `package.json`과 `package-lock.json`을 `0.0.18`로 올렸습니다.
- `programmersHelper.executionMode`가 `docker`에서 `local`로 바뀌면 helper Docker 컨테이너를 자동으로 중지하고, Docker 정리 명령에 제한 시간을 두며 사이드바에 결과를 표시합니다.
- `local`에서 `docker`로 다시 바뀔 때는 가벼운 전환 안내만 표시하고, Docker 컨테이너는 다음 문제 열기나 테스트 실행 시점에 준비합니다.
- 활성 C++ 테스트 대상을 `solution.cpp`와 저장된 `solution-*.cpp` 스냅샷으로 제한해 생성된 `test_runner.cpp`가 실수로 실행되지 않도록 했습니다.
- 문제 레이아웃 정리 중 현재 `Programmers` 루트 밖의 관련 없는 C++ 탭이 닫힐 수 있는 경로 계산을 수정했습니다.
- 문제 목록 인덱스와 풀이 스냅샷 경로를 사용하기 전에 검증해, 오래된 인덱스는 재생성하고 안전하지 않은 스냅샷 메타데이터는 무시합니다.
- 사이드바 문제 검색 렌더링에 debounce를 적용해 연속 입력 중 불필요한 DOM 재생성을 줄였습니다.
- `docs/feature-flows.md`에 실행 모드 변경, 실행 대상 선택, 문제 인덱스, 스냅샷 경로 안전성 흐름을 보강했습니다.

## 0.0.17

- 초기화나 새 풀이 시작 시 오른쪽 풀이 에디터를 안정적으로 유지해 화면 깜빡임을 줄였습니다.
- 저장된 풀이 스냅샷을 테스트할 때 선택된 소스 include 경로를 컴파일 fingerprint에 포함해 오래된 바이너리가 재사용되지 않도록 수정했습니다.
- 문제 전환 후 현재 Markdown 미리보기와 C++ 풀이 탭이 유지되도록 문제 레이아웃 정리 로직을 보완했습니다.

## 0.0.16

- 확장 활성화 시점의 무거운 초기화를 실제 사용 시점으로 미뤄 사이드바가 더 빨리 뜨도록 개선했습니다.
- Webview 내부 로딩바를 제거하고, 사이드바 준비 중에는 VS Code 진행 표시를 사용하도록 정리했습니다.
- 문제 목록을 단일 인덱스 파일로 캐시해 목록 로딩 비용을 줄이고, 생성/삭제/다시풀기 변경 후 목록이 바로 갱신되도록 보완했습니다.
- `.programmers-helper` 내부 파일이 문제 목록에 섞여 보이지 않도록 필터링했습니다.
- 새 풀이와 초기화 시 전체 에디터를 다시 열지 않고 오른쪽 풀이 탭을 유지해 화면 깜빡임을 줄였습니다.
- 문제 전환 후 왼쪽에는 현재 문제 Markdown, 오른쪽에는 현재 풀이 C++만 남도록 탭 정리를 보완했습니다.
- 저장된 풀이 스냅샷을 실행할 때 현재 보고 있는 C++ 파일을 테스트 대상으로 사용하고, 컴파일 캐시에 소스 파일 경로를 포함해 잘못된 바이너리 재사용을 막았습니다.

## 0.0.11

- 샘플 테스트 실행 시 저장된 `programmers.json`의 예제를 우선 사용하고, 필요할 때만 `problem.md`에서 가져와 저장합니다.
- 사이드바의 샘플/커스텀 테스트는 명시적으로 열린 현재 문제가 있을 때만 실행되도록 했습니다.
- 확장 세션 동안 Docker 준비 확인 결과를 캐시하고, 캐시된 경로가 실패하면 새로 확인해 재시도합니다.
- 풀이 코드, 생성된 테스트 러너 코드, 컴파일 플래그가 바뀌지 않았으면 기존 컴파일 바이너리를 재사용합니다.
- 테스트 결과를 케이스별로 집계해 전체 출력 문자열을 누적하지 않도록 메모리 사용을 줄였습니다.
- 사이드바 문제 요약을 메모리에 캐시하고, 명시적 새로고침이나 파일 변경 동작 후에만 전체를 다시 스캔합니다.
- 출력 캡처 버퍼 수정으로 `clang++` 컴파일 오류 상세 출력이 다시 보이도록 복구했습니다.

## 0.0.10

- 사이드바에 문제별 `notes.md` 메모를 여는 동작을 추가했습니다.
- 새 메모 파일을 만들 때 문제 정보가 들어간 학습 노트 템플릿을 생성합니다.
- 메모 파일을 열어도 문제 Markdown 미리보기가 바뀌지 않도록 미리보기를 잠급니다.

## 0.0.9

- 사이드바의 문제 열기 / 현재 상태 섹션을 접을 수 있게 하고, 접힌 상태에서 상태 요약을 표시합니다.

## 0.0.8

- 테스트 실행 시간을 소수점 둘째 자리까지 표시하고 결과 줄에 메모리 사용량을 함께 표시합니다.
- C++ 컴파일과 테스트 실행을 Docker 런타임 컨테이너에서 수행합니다.
- 확장 런타임 코드를 역할별 모듈로 정리했습니다.
- 문제별 풀이 기록을 추가하고 새 풀이, 열기, 삭제, 초기화 흐름을 지원합니다.
- 저장된 풀이를 열어 둔 상태에서는 현재 보고 있는 C++ 파일을 대상으로 테스트를 실행합니다.
- 확장이 비활성화될 때 Docker 실행 컨테이너를 정리합니다.
- 현재 문제 관련 동작을 문제 목록 밖으로 옮기고 사이드바의 릴리스 메타데이터 표시를 개선했습니다.

## 0.0.7

- 로컬 설치 빌드와 분리된 환경에서 확장을 개발할 수 있도록 Dev Container 워크플로를 추가했습니다.
- 원격 및 컨테이너 세션에서 워크스페이스 확장 호스트에서 실행되도록 확장을 표시했습니다.
- 컨테이너 환경에 맞는 디버그 태스크와 실행 설정을 추가했습니다.
- 문제별 커스텀 테스트 케이스를 저장하고 문제를 다시 열 때 복원합니다.
- 사이드바에 문제 검색을 추가하고 테스트 동작 배치를 개선했습니다.
- 런타임 오류와 컴파일 실패를 구분하고 `clang++` 누락 안내를 더 명확하게 표시합니다.
- 긴 URL 때문에 레이아웃이 깨지지 않도록 사이드바의 긴 상태 메시지를 줄바꿈합니다.
- 휴지통을 사용할 수 없을 때의 문제 삭제 fallback 동작을 개선했습니다.

## 0.0.6

- 로컬 `Programmers` 폴더를 사용할 수 없을 때 VS Code 전역 저장소를 기본 문제 저장 위치로 사용합니다.
- 확인 절차와 휴지통 지원을 포함한 문제 폴더 삭제 동작을 추가했습니다.
- 활성 에디터 경로를 테스트 대상으로 사용하기 전에 검증합니다.
- 문제 페이지를 크롤링할 때 프로그래머스 리다이렉트와 응답 크기를 제한합니다.
- 긴 로컬 경로를 숨겨 컴파일 오류 요약을 더 읽기 쉽게 개선했습니다.
- C++ 테스트가 현재 사용자 권한으로 실행된다는 점을 문서화했습니다.

## 0.0.5

- 테스트 케이스별 제한 시간과 테스트 실행 중지 버튼을 추가했습니다.
- 전체 로그는 Output 패널에 유지하면서 사이드바에는 간결한 컴파일 오류 요약을 표시합니다.
- 제한 시간이 전체 테스트 묶음을 막지 않도록 생성된 테스트 케이스를 하나씩 실행합니다.
- 누락된 include가 가려지지 않도록 runner helper header보다 먼저 `solution.cpp`를 컴파일합니다.
- `Programmers/Programmers`를 만들지 않고 열려 있는 `Programmers` 폴더를 직접 사용합니다.

## 0.0.4

- 기존 로컬 문제에 이미 `problem.md`와 `solution.cpp`가 있으면 프로그래머스 크롤링을 건너뜁니다.
- 로컬 문제를 병렬로 불러와 문제 목록 새로고침 속도를 높였습니다.
- 더 이상 문제 파일이 없는 오래된 마지막 문제 경로를 무시합니다.
- 준비 상태와 테스트 완료 상태에 대한 사이드바 피드백을 단순화했습니다.

## 0.0.3

- 사이드바를 고정 영역, 테스트 영역, 문제 목록 영역으로 분리했습니다.
- 접을 수 있는 테스트 섹션과 문제 목록 섹션을 추가했습니다.
- 리뷰 토글을 문제 행 안으로 옮겼습니다.
- 준비 상태 텍스트와 간격을 줄였습니다.

## 0.0.2

- `.programmers-helper/review.json`에 저장되는 현재 문제 리뷰 체크박스를 추가했습니다.
- 사이드바에 접을 수 있는 리뷰 목록과 전체 문제 목록을 추가했습니다.
- 생성, 마지막 문제, 리뷰 목록, 전체 목록 동작에서 문제를 일관되게 열도록 개선했습니다.
- 문제를 열 때마다 커스텀 테스트 기본값과 준비 상태를 새로고침합니다.

## 0.0.1

- Programmers 사이드바 뷰를 추가했습니다.
- 프로그래머스 lesson 번호로 `problem.md`, `solution.cpp`, `programmers.json`를 생성합니다.
- 왼쪽에는 Markdown 미리보기, 오른쪽에는 C++ 풀이 파일을 엽니다.
- `problem.md`에서 파싱한 샘플 테스트를 실행합니다.
- Input / Expected Output 필드에 입력한 커스텀 테스트를 실행합니다.
